---
import ArticleLayout from '../../layouts/ArticleLayout.astro';
import { getArticlePageData, getArticleStaticPaths } from '../../utils/articleRoute';

export const getStaticPaths = () => getArticleStaticPaths('prompts');

const { entry } = Astro.props;
const { Content, headings, related } = await getArticlePageData(entry, 'prompts');
---
<ArticleLayout entry={entry} collection="prompts" headings={headings} body={entry.body ?? ''} related={related}>
  <Content />
</ArticleLayout>
