---
import ArticleLayout from '../../layouts/ArticleLayout.astro';
import { getArticlePageData, getArticleStaticPaths } from '../../utils/articleRoute';

export const getStaticPaths = () => getArticleStaticPaths('tools');

const { entry } = Astro.props;
const { Content, headings, related } = await getArticlePageData(entry, 'tools');
---
<ArticleLayout entry={entry} collection="tools" headings={headings} body={entry.body ?? ''} related={related}>
  <Content />
</ArticleLayout>
