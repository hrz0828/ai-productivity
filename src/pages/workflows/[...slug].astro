---
import ArticleLayout from '../../layouts/ArticleLayout.astro';
import { getArticlePageData, getArticleStaticPaths } from '../../utils/articleRoute';

export const getStaticPaths = () => getArticleStaticPaths('workflows');

const { entry } = Astro.props;
const { Content, headings, related } = await getArticlePageData(entry, 'workflows');
---
<ArticleLayout entry={entry} collection="workflows" headings={headings} body={entry.body ?? ''} related={related}>
  <Content />
</ArticleLayout>
