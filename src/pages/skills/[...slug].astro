---
import ArticleLayout from '../../layouts/ArticleLayout.astro';
import { getArticlePageData, getArticleStaticPaths } from '../../utils/articleRoute';

export const getStaticPaths = () => getArticleStaticPaths('skills');

const { entry } = Astro.props;
const { Content, headings, related } = await getArticlePageData(entry, 'skills');
---
<ArticleLayout entry={entry} collection="skills" headings={headings} body={entry.body ?? ''} related={related}>
  <Content />
</ArticleLayout>
